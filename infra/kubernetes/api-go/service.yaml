apiVersion: v1
kind: Service
metadata:
  name: go-api
spec:
  selector:
    app: go-api
  ports:
    - protocol: TCP
      port: 8081      # Port inside the cluster
      targetPort: 8081 # Port the app listens on
      nodePort: 30081  # Port you can access on localhost:PORT
  type: NodePort